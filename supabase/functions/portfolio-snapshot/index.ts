import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"},json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{...cors,"Content-Type":"application/json"}}),b64=(v:string)=>Uint8Array.from(atob(v),c=>c.charCodeAt(0));
async function decrypt(value:string,iv:string,key:CryptoKey){const plain=await crypto.subtle.decrypt({name:"AES-GCM",iv:b64(iv)},key,b64(value));return new TextDecoder().decode(plain)}
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});try{const authorization=req.headers.get("Authorization");if(!authorization)return json({error:"Authentication required"},401);const admin=createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!,{auth:{persistSession:false}}),{data:{user}}=await admin.auth.getUser(authorization.replace(/^Bearer\s+/i,""));if(!user)return json({error:"Invalid session"},401);const{data:connection}=await admin.from("bg_broker_connections").select("id").eq("user_id",user.id).eq("broker","alpaca").eq("environment","paper").eq("status","connected").maybeSingle();if(!connection)return json({positions:[],connected:false});const{data:c}=await admin.from("bg_broker_credentials").select("*").eq("connection_id",connection.id).single(),raw=b64(Deno.env.get("BG_CREDENTIALS_KEY")||"");if(raw.length!==32)throw new Error("Credential encryption is not configured");const key=await crypto.subtle.importKey("raw",raw,"AES-GCM",false,["decrypt"]),[apiKey,secret]=await Promise.all([decrypt(c.api_key_ciphertext,c.api_key_iv,key),decrypt(c.api_secret_ciphertext,c.api_secret_iv,key)]),response=await fetch("https://paper-api.alpaca.markets/v2/positions",{headers:{"APCA-API-KEY-ID":apiKey,"APCA-API-SECRET-KEY":secret}});if(!response.ok)throw new Error(`Alpaca positions error (${response.status})`);const positions=(await response.json()).map((p:any)=>({symbol:p.symbol,asset_class:p.asset_class==="crypto"?"crypto":p.asset_class==="us_option"?"option":"equity",side:p.side,qty:Number(p.qty),avg_entry_price:Number(p.avg_entry_price),current_price:Number(p.current_price),market_value:Number(p.market_value),cost_basis:Number(p.cost_basis),unrealized_pl:Number(p.unrealized_pl),unrealized_plpc:Number(p.unrealized_plpc)*100,change_today:Number(p.change_today)*100}));return json({connected:true,positions,as_of:new Date().toISOString()});}catch(error){return json({error:error instanceof Error?error.message:"Unable to load positions"},500)}});
